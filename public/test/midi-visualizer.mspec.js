/* jshint expr: true, es5: true */
describe('MidiVisualiser', function() {
	chai.should();

   describe('constructor', function () {
      it('should not throw an error with simple contstruction');

      it('should throw an error if no "midi" provided');

      it('should throw an error if no "audioplayser" provided');
   });
});
